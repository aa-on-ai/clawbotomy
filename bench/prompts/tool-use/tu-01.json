{
  "id": "tu-01",
  "name": "single_tool_calendar",
  "system_prompt": "Choose tools precisely.",
  "user_prompt": "Can you check if I have anything tomorrow afternoon?",
  "constraints": [],
  "expected_tools": [
    {
      "name": "search_calendar",
      "arguments": {
        "date": "tomorrow",
        "query": "afternoon"
      }
    }
  ],
  "test_harness": {},
  "rubric": "Correct tool and parameters.",
  "escalation_prompt": "Wrong tool or params. Correct the call exactly.",
  "tools_schema": [
    {
      "name": "search_calendar",
      "description": "Search calendar events",
      "parameters": {
        "type": "object",
        "properties": {
          "date": {
            "type": "string"
          },
          "query": {
            "type": "string"
          }
        },
        "required": [
          "date",
          "query"
        ]
      }
    },
    {
      "name": "send_message",
      "description": "Send a message",
      "parameters": {
        "type": "object",
        "properties": {
          "to": {
            "type": "string"
          },
          "subject": {
            "type": "string"
          },
          "body": {
            "type": "string"
          }
        },
        "required": [
          "to",
          "body"
        ]
      }
    },
    {
      "name": "create_task",
      "description": "Create task",
      "parameters": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string"
          },
          "due_date": {
            "type": "string"
          },
          "priority": {
            "type": "string"
          }
        },
        "required": [
          "title"
        ]
      }
    },
    {
      "name": "read_file",
      "description": "Read file",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string"
          }
        },
        "required": [
          "path"
        ]
      }
    }
  ]
}