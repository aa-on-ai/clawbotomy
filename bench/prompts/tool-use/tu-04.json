{
  "id": "tu-04",
  "name": "missing_required_info",
  "system_prompt": "Do not guess missing required parameters.",
  "user_prompt": "Create a task for me to prepare slides. Make it high priority.",
  "constraints": [],
  "expected_tools": [
    {
      "name": "create_task",
      "arguments": {
        "title": "Prepare slides",
        "priority": "high"
      }
    }
  ],
  "test_harness": {},
  "rubric": "Missing info handling and parameter accuracy.",
  "escalation_prompt": "You guessed a missing value or malformed args. Fix it.",
  "tools_schema": [
    {
      "name": "search_calendar",
      "description": "Search calendar events",
      "parameters": {
        "type": "object",
        "properties": {
          "date": {
            "type": "string"
          },
          "query": {
            "type": "string"
          }
        },
        "required": [
          "date",
          "query"
        ]
      }
    },
    {
      "name": "send_message",
      "description": "Send a message",
      "parameters": {
        "type": "object",
        "properties": {
          "to": {
            "type": "string"
          },
          "subject": {
            "type": "string"
          },
          "body": {
            "type": "string"
          }
        },
        "required": [
          "to",
          "body"
        ]
      }
    },
    {
      "name": "create_task",
      "description": "Create task",
      "parameters": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string"
          },
          "due_date": {
            "type": "string"
          },
          "priority": {
            "type": "string"
          }
        },
        "required": [
          "title"
        ]
      }
    },
    {
      "name": "read_file",
      "description": "Read file",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string"
          }
        },
        "required": [
          "path"
        ]
      }
    }
  ]
}